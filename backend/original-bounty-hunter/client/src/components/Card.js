import React,{Component} from 'react';

class Card extends Component{
    render(){
        return(
            <div>
                <input name="firstName" value={ this.state.firstName } onChange={} type='text'/>            
                
            </div>
        )
    }
}

export default Card;